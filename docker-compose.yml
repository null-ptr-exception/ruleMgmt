services:
  app:
    build: .
    ports:
      - "127.0.0.1:${GITHUB_PR_PREVIEW_PORT:-3001}:3000"
    environment:
      - PORT=3000
      - PUBLIC_URL=${GITHUB_PR_PREVIEW_URL:-http://localhost:3001}
